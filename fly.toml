# fly.toml app configuration file generated for nicoufish on 2024-08-30T16:37:05+09:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'nicoufish'
primary_region = 'nrt'

[build]

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '256mb'
  size = 'shared-cpu-1x'
  #cpu_kind = 'shared'
  #cpus = 1
